/*
export interface Role extends Creep {
    memory: RoleMemory;
  }

  interface RoleMemory extends CreepMemory {
    role: 'role';
  }

  const roleRole = {
    run(creep: Role): void {
    }
  };

  export default roleRole;
  */
